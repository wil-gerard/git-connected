[build]
  # Directory that contains the deploy-ready HTML files
  # and assets generated by the build..
  publish = "build"
  command = "CI=false yarn build"
  
[[redirects]]
    from = "/*"    
    to = "/index.html" 
    status =  200